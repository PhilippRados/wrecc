int printf(char* s, int d);

int main() {
  long two = 2;
  int four = 4;

  int result = 9 * two + 2000 / four * (two+3 + 6) / (7 * 8 + four+5 / 10 * 11 / two * 4 + 1 * ((long)1 + two) * (four % (char)two));
  int z = 32;
  int result2 = !!9 * two + 2000 / four * (5 + 6) / (7 * (four + 4 + 9) / 10 * 11 / two * four + 1 * ((long)1 + two) 
          * (four % (z += 1 * two-1))) + z++;
  /* int result2 = !!9 * 2 + 2000 / 4 * (5 + 6) / (7 * (8 + 9) / 10 * 11 / 2 * 4 + 1 * ((long)1 + 2) * (4 % (z += 1 * 1))) + z++;// - 1); */

  printf("%d\n", z);

  int a = 256;
  int* b = &a;
  printf("%d\n", result);
  printf("%d\n", result2);
  printf("%d\n", *b << *b-- * two | (two % 10) == 4 != *(3 + b) + (four % 2 * -1 + (long)*b + !a++));
}
